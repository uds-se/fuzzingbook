This folder contains files shared between the `fuzzingbook` and `debuggingbook` projects. 

The master copy is in `debuggingbook`. Please make changes there and then use `make shared` to copy things over.